import { Container } from "@chakra-ui/react";
import UpdateProfileForm from "components/sections/UpdateProfileForm";

const Profile = () => {
  return (
    <Container mt="4">
      <UpdateProfileForm />
    </Container>
  );
};

export default Profile;
